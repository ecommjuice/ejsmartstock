<div class="ss-topbar">
  <div class="ss-topbar-brand">
    <span class="ss-logo-icon">SS</span>
    <span class="ss-logo-text">EJ Smart <strong>stock</strong></span>
  </div>
  <nav class="ss-nav">
    <a href="{$link_scan}" class="ss-nav-item {if $active_tab == 'scan'}active{/if}"><span class="ss-nav-icon">SCAN</span> Escaner</a>
    <a href="{$link_history}" class="ss-nav-item {if $active_tab == 'history'}active{/if}"><span class="ss-nav-icon">HIS</span> Historial</a>
    <a href="{$link_inventory}" class="ss-nav-item {if $active_tab == 'inventory'}active{/if}"><span class="ss-nav-icon">INV</span> Inventario</a>
    <a href="{$link_status}" class="ss-nav-item {if $active_tab == 'status'}active{/if}"><span class="ss-nav-icon">STK</span> Estado stock</a>
    <a href="{$link_import}" class="ss-nav-item {if $active_tab == 'import'}active{/if}"><span class="ss-nav-icon">CSV</span> Importar CSV</a>
  </nav>
</div>
